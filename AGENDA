BUG / TODO Liste:

* Type classes

* Typen in instantiations, alpha -> "(alpha :: typ)"

* foo :: Context a -> Foo a -> Bar b
    
    => foo :: Foo (a::Context) -> Bar (b::Context)

* Characters & Strings:

* Adaption OpKind fuer Class mappings


definition foo :: string
where
  "foo = ''asdf''"

definition bar :: char
where
  "bar = CHR ''a''"



$Id$
